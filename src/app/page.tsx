import { GameShell } from "@/components/game/GameShell";

export default function Home() {
  return (
    <main className="min-h-screen bg-[var(--roxana-bg)] text-[var(--roxana-text)]">
      <GameShell />
    </main>
  );
}
